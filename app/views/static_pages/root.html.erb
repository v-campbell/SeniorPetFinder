<%# Bootstrap current user to prevent initial redering of a 'not-logged-in' state before receiving current user %>
<% if logged_in? %>
  <script id="bootstrap-current-user" type="text/javascript">
      window.currentUser = 
      <%= render(
          "api/users/user.json.jbuilder", 
          user: current_user
          ).html_safe %>
  </script>
<% end %>

<div id="root">I think React is broken</div>